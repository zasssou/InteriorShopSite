$(function() {

  $('.c-hamburger-menu').on('click', () => {
    $('#header').toggleClass('open-menu');
  });

  $('.c-overlay').on('click', () => {
    $('#header').toggleClass('open-menu');
  });

});
